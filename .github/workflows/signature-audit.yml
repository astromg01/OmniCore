name: Retired Signature Audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Signing audit completed; stable DEV certificate is now enforced by android.yml."
