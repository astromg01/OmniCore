name: OmniCore Identity Migration (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Migration complete
        run: echo "OmniCore identity is now Mauricio.gamedev / @mauricio-gamedev."
