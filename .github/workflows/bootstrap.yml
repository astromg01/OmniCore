name: Android Build (legacy path)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Legacy workflow path
        run: echo "This temporary workflow path is being replaced by android.yml."
