package com.omnicore.emulator.core.ps2

/**
 * Temporary source-compatibility alias while the PS2 Activity is migrated away
 * from its historical Play!-specific constructor name.
 *
 * Alpha 6 no longer boots Play!: every call resolves to Pcsx2PS2Backend.
 */
typealias PlayPS2Backend = Pcsx2PS2Backend
